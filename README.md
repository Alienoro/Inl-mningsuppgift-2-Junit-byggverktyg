Jag använde mig utav mvn compile för att kompilera klasserna och så att dem lades till under target/classes.
Sedan använde jag mig av mvn test för att testa klasserna så att det funkade utan problem.
Sedan använde jag mig utav mvn package som skapar jar filer för hela projektet.
Sist men inte minst så använde jag mig utav mvn site som skapar javadoc.

@author Sebastian Magnusson
